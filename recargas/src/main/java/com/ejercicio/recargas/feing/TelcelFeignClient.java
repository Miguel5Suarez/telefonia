package com.ejercicio.recargas.feing;

import org.springframework.cloud.openfeign.FeignClient;
import org.springframework.web.bind.annotation.PostMapping;

/*@FeignClient(name = "telcelClient", url = "https://api.telcel.com")
public interface TelcelFeignClient {
	@PostMapping("/telcel/recarga")
	void comparPaquete(String numeroTelefono, String carrier, int monto);

}*/
